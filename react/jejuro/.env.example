# 이 파일을 복사해 .env.local 로 만들고 값을 채운다 (.env.local 은 git에 올리지 않는다)
# 카카오 개발자 콘솔 > 내 애플리케이션 > 앱 키 > JavaScript 키
# React(Create React App)는 이름이 REACT_APP_ 으로 시작하는 값만 읽는다. 수정 후 npm start 재시작.
REACT_APP_KAKAO_JS_KEY=